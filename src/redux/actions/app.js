import actionTypes from '../constants';

export const showProfile = () => ({
  type: actionTypes.ACTION_SHOW_PROFILE,
});

export const hideProfile = () => ({
  type: actionTypes.ACTION_HIDE_PROFILE,
});
